1. Descargar la imagen de docker.
    $ docker pull facundoguarnier/computacion-2_2022:<version>

2. Ejecutar un contenedor con la imagen descargada en modo interactivo: 
    $ docker run -it -p <puerto>:<puerto> -e p=<puerto> nombre_imagen
    Donde <puerto> es el puerto que se va a tulizar en el server.

3. Ejecutar el servidor ubicado en la carpeta app/server: $ python3 mi_server.py -p <puerto> -i4 <ipv4> -i6 <ipv6> 